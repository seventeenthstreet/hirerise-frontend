// Stub — replace with real step-sequencing check
import React from 'react';
export default function OnboardingGuard({ children }: { children: React.ReactNode; requiredStep?: string }) {
  return <>{children}</>;
}
