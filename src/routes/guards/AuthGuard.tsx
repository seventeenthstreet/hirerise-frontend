// Stub — replace with real Supabase session check
import React from 'react';
export default function AuthGuard({ children }: { children: React.ReactNode; requireOnboarding?: boolean }) {
  return <>{children}</>;
}
